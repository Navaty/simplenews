<div class="container sticky-top">
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  	<%= link_to "Kazan news", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#">Главная <span class="sr-only">(current)</span></a>
      </li>
    </ul>
          <div class="nav-item my-2 my-lg-0">
          	<ul class="navbar-nav mr-auto">
        <% if user_signed_in? %> 
        	<%= link_to 'Добавить новость', new_post_path, class: "nav-link my-2 my-sm-0" %>
        	<%= link_to 'Выйти', destroy_user_session_path, method: :delete, class: "nav-link my-2 my-sm-0" %>
        <% else %>
            <%= link_to 'Войти', new_user_session_path, class: "nav-link my-2 my-sm-0" %>
            <%= link_to 'Регистрация', new_user_registration_path, class: "nav-link my-2 my-sm-0" %>
        <% end %>
        </ul>
      </div>
  </div>
</nav>
</div>